workspace: simple-http-bot

providers:
  openai:
    api_key: env:OPENAI_API_KEY
    max_tokens: 1024

adapters:
  http:
    api_key: env:BOT_HTTP_KEY

bots:
  - id: simple-bot
    model: openai:gpt-4o-mini
    system_prompt: prompts/system.md
    channels:
      - http:/api/chat
